class Burger < ApplicationRecord
end
class Burger < ApplicationRecord
    def nutiments
        # USE -> product.nutriments.to_hash
        # Send nutiments to controler
        # Make controler give it to the view (display only the "show burger" view)
        # add it to json
    end
end